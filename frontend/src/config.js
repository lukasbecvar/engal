// URL to API access
export const API_URL = "http://engal.localhost/"

// token to access API data
export const API_TOKEN = "1234"

// maximum items can loaded on page
export const MAX_ITEMS_PER_PAGE = 20

// media encryption token (encrypt & decrypt only in client) [!!if this = null encryption disable!!]
export const ENCRYPTION_TOKEN = null